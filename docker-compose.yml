services:
  frontend:
    build: .
    restart: unless-stopped
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_BACKEND_URL=http://dtg-be-app:8000
    networks:
      - dtg-network

networks:
  dtg-network:
    external: true
